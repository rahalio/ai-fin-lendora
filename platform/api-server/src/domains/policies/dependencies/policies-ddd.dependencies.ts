/**
 * Policies DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: policies
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Policies Domain Module - Composition root
 *
 * DDD: Composition root for policies domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ApproveRepositoryAdapter,
  PolicyRepositoryAdapter,
  ReasonCodeRepositoryAdapter,
  RollbackRepositoryAdapter,
  SimulateRepositoryAdapter,
  SubmitApprovalRepositoryAdapter,
} from "@lendora/adapters/policies";
import { getIdGeneratorService } from "@lendora/adapters";
import type { AdapterDynamoDBClient } from "@lendora/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteApprovePolicyPack,
  ExecuteCreatePolicyPack,
  ExecuteCreateReasonCode,
  ExecuteGetPolicyPack,
  ExecuteGetReasonCode,
  ExecuteListPolicyPacks,
  ExecuteListReasonCodes,
  ExecutePublishReasonCode,
  ExecuteRollbackPolicyPack,
  ExecuteSubmitPolicyPackApproval,
  ExecuteUpdatePolicyPack,
  ExecuteUpdateReasonCode,
} from "@lendora/services/policies/usecases";
// Ports (interfaces)
import type {
  ApproveRepository,
  PolicyRepository,
  ReasonCodeRepository,
  RollbackRepository,
  SimulateRepository,
  SubmitApprovalRepository,
} from "@lendora/services/policies/ports";

export interface PoliciesDomainModule {
  repos: {
    approves: ApproveRepository;
    policies: PolicyRepository;
    reasonCodes: ReasonCodeRepository;
    rollbacks: RollbackRepository;
    simulates: SimulateRepository;
    submitApprovals: SubmitApprovalRepository;
  };
  useCases: {
    approves: {
      create: ExecuteApprovePolicyPack;
    };
    policies: {
      create: ExecuteCreatePolicyPack;
      get: ExecuteGetPolicyPack;
      list: ExecuteListPolicyPacks;
      update: ExecuteUpdatePolicyPack;
    };
    reasonCodes: {
      create: ExecuteCreateReasonCode;
      get: ExecuteGetReasonCode;
      list: ExecuteListReasonCodes;
      update: ExecuteUpdateReasonCode;
    };
    rollbacks: {
      get: ExecuteRollbackPolicyPack;
    };
    submitApprovals: {
      create: ExecuteSubmitPolicyPackApproval;
    };
  };
}

export function buildPoliciesDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): PoliciesDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      approves?: ApproveRepository;
      policies?: PolicyRepository;
      reasonCodes?: ReasonCodeRepository;
      rollbacks?: RollbackRepository;
      simulates?: SimulateRepository;
      submitApprovals?: SubmitApprovalRepository;
    } = {};

    obj.approves = new ApproveRepositoryAdapter(dynamoClient);
    obj.policies = new PolicyRepositoryAdapter(dynamoClient);
    obj.reasonCodes = new ReasonCodeRepositoryAdapter(dynamoClient);
    obj.rollbacks = new RollbackRepositoryAdapter(dynamoClient);
    obj.simulates = new SimulateRepositoryAdapter(dynamoClient);
    obj.submitApprovals = new SubmitApprovalRepositoryAdapter(dynamoClient);
    return {
      approves: obj.approves!,
      policies: obj.policies!,
      reasonCodes: obj.reasonCodes!,
      rollbacks: obj.rollbacks!,
      simulates: obj.simulates!,
      submitApprovals: obj.submitApprovals!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    approves: {
      create: new ExecuteApprovePolicyPack(executionContext, idGenerator, repos.approves),
    },
    policies: {
      create: new ExecuteCreatePolicyPack(executionContext, idGenerator, repos.policies),
      get: new ExecuteGetPolicyPack(executionContext, idGenerator, repos.policies),
      list: new ExecuteListPolicyPacks(executionContext, idGenerator, repos.policies),
      update: new ExecuteUpdatePolicyPack(executionContext, idGenerator, repos.policies),
    },
    reasonCodes: {
      create: new ExecuteCreateReasonCode(executionContext, idGenerator, repos.reasonCodes),
      get: new ExecuteGetReasonCode(executionContext, idGenerator, repos.reasonCodes),
      list: new ExecuteListReasonCodes(executionContext, idGenerator, repos.reasonCodes),
      update: new ExecuteUpdateReasonCode(executionContext, idGenerator, repos.reasonCodes),
    },
    rollbacks: {
      get: new ExecuteRollbackPolicyPack(executionContext, idGenerator, repos.rollbacks),
    },
    submitApprovals: {
      create: new ExecuteSubmitPolicyPackApproval(executionContext, idGenerator, repos.submitApprovals),
    },
  };
  return { repos, useCases: useCases as PoliciesDomainModule["useCases"] };
}
