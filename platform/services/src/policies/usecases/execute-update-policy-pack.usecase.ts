/**
 * ExecuteUpdatePolicyPack - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: policies
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteUpdatePolicyPack Use Case
 *
 * DDD: Application use case for update Policy.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { UpdatePolicyPackInput, UpdatePolicyPackOutput } from "../dto/policy.dto";
import type { ExecutionContextService, IdGeneratorService } from "@lendora/services/_shared/index.js";
import type { PolicyRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanUpdate } from "../policies/can-update.policy";
export class ExecuteUpdatePolicyPack {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly policy: PolicyRepository
  ) {}

  async execute(input: UpdatePolicyPackInput): Promise<UpdatePolicyPackOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.polId();

    // All field names extracted from OpenAPI spec
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).policyPackId) {
      throw new ValidationError("policyPackId is required");
    }
    const policypackId = (input as any).policyPackId;
    // No get/list operation for this resource - call update directly
    // Check policy
    if (!CanUpdate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Update entity via repository (pass correlation ID for response meta)
    const updated = await this.policy.updatePolicyPack({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return updated as UpdatePolicyPackOutput;
  }
}
