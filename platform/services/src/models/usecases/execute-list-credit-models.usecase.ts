/**
 * ExecuteListCreditModels - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: models
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListCreditModels Use Case
 *
 * DDD: Application use case for list VModel.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListCreditModelsInput, ListCreditModelsOutput } from "../dto/vmodel.dto";
import type { ExecutionContextService, IdGeneratorService } from "@lendora/services/_shared/index.js";
import type { VModelRepository } from "../ports";

export class ExecuteListCreditModels {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly vModel: VModelRepository
  ) {}

  async execute(input: ListCreditModelsInput): Promise<ListCreditModelsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.mdlId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.vModel.listCreditModels({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListCreditModelsOutput;
  }
}
