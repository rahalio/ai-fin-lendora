/**
 * ExecuteGetCurrentLearningEnvelope - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: models
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetCurrentLearningEnvelope Use Case
 *
 * DDD: Application use case for get Current.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetCurrentLearningEnvelopeInput, GetCurrentLearningEnvelopeOutput } from "../dto/current.dto";
import type { ExecutionContextService, IdGeneratorService } from "@lendora/services/_shared/index.js";
import type { CurrentRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetCurrentLearningEnvelope {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly current: CurrentRepository
  ) {}

  async execute(input: GetCurrentLearningEnvelopeInput): Promise<GetCurrentLearningEnvelopeOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.mdlId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.current.getCurrentLearningEnvelope({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetCurrentLearningEnvelopeOutput;
  }
}
