/**
 * ExecuteGetReferTask - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: referrals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetReferTask Use Case
 *
 * DDD: Application use case for get ReferTask.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetReferTaskInput, GetReferTaskOutput } from "../dto/refer-task.dto";
import type { ExecutionContextService, IdGeneratorService } from "@lendora/services/_shared/index.js";
import type { ReferTaskRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetReferTask {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly referTask: ReferTaskRepository
  ) {}

  async execute(input: GetReferTaskInput): Promise<GetReferTaskOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.refId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).referTaskId) {
      throw new ValidationError("referTaskId is required");
    }
    const refertaskId = (input as any).referTaskId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.referTask.getReferTask({ referTaskId: refertaskId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${refertaskId}`);
    }

    return entity as GetReferTaskOutput;
  }
}
