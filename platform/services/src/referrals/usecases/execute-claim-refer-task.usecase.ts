/**
 * ExecuteClaimReferTask - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: referrals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteClaimReferTask Use Case
 *
 * DDD: Application use case for get Claim.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ClaimReferTaskInput, ClaimReferTaskOutput } from "../dto/claim.dto";
import type { ExecutionContextService, IdGeneratorService } from "@lendora/services/_shared/index.js";
import type { ClaimRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteClaimReferTask {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly claim: ClaimRepository
  ) {}

  async execute(input: ClaimReferTaskInput): Promise<ClaimReferTaskOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.refId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).referTaskId) {
      throw new ValidationError("referTaskId is required");
    }
    const refertaskId = (input as any).referTaskId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.claim.claimReferTask({ referTaskId: refertaskId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${refertaskId}`);
    }

    return entity as ClaimReferTaskOutput;
  }
}
