/**
 * GetCurrentLearningEnvelope handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: models
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getCurrentLearningEnvelope } from "@lendora/api-server/domains/models/handlers";
import type { ModelsDDDDependencies } from "@lendora/api-server/domains/models";
import { createCurrent as createCurrentEntity } from "@lendora/factories/models";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@lendora/tests/shared";
import { MODELS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetCurrentLearningEnvelope", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ModelsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ModelsDDDDependencies>(
      MODELS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity", async () => {
    const entity = createCurrentEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetCurrentLearningEnvelope, "execute").mockResolvedValue(useCaseResult);

    await getCurrentLearningEnvelope(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetCurrentLearningEnvelope.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
