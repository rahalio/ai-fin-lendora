/**
 * GetDecisionAuditByApplication handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: audits
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getDecisionAuditByApplication } from "@lendora/api-server/domains/audits/handlers";
import type { AuditsDDDDependencies } from "@lendora/api-server/domains/audits";
import { createByApplication as createByApplicationEntity } from "@lendora/factories/audits";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@lendora/tests/shared";
import { AUDITS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetDecisionAuditByApplication", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AuditsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AuditsDDDDependencies>(
      AUDITS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createByApplicationEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetDecisionAuditByApplication, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { applicationId: entity.id };

    await getDecisionAuditByApplication(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetDecisionAuditByApplication.execute).toHaveBeenCalledWith(expect.objectContaining({ applicationId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { applicationId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetDecisionAuditByApplication, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getDecisionAuditByApplication(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
